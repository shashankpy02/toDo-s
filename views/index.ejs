<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-800 rounded-lg w-full text-white" type="text" placeholder="enter the title here" name="title">
                <textarea class="block px-5 py-2 bg-zinc-800 resize-none mt-2 rounded-lg w-full text-white" placeholder="enter your task details here" name="details"></textarea>
                <input type="submit" class="mt-2 rounded-lg bg-blue-500 text-white px-2 py-2 cursor-pointer">
            </form>
        </div>

        <div class="tasks p-10 text-white text-2xl flex gap-3 flex-wrap">

            <% if(files.length > 0){ %>

                <% files.forEach((file)=>{ %>
                    <div class="card min-w-72 w-72 px-3 py-4 rounded-md bg-zinc-800 tracking-tighter">
                        <h1><%= file %></h1>
                        <div class="flex justify-between item-center mt-3">
                            <a class="text-blue-500" href="/file/<%= file %>">read more</a>
                            <a class="text-zinc-500" href="/edit/<%= file %>">Edit</a>
                        </div>
                        <a class="text-zinc-500 text-sm" href="/delete/<%= file %>">delete❌</a>
                    </div>
                <% }) %>

            <% }else{ %>
                    <h1 class="text-zinc-500">No Tasks yet</h1>
            <% } %>

            
        </div>
    </div>
</body>
</html>